namespace LMP;

/// <summary>
/// Optimization parameter space descriptor. Phase A: stub.
/// Full implementation (all 6 parameter kinds) added in Phase C.
/// </summary>
public sealed class TypedParameterSpace
{
    /// <summary>Empty parameter space (no parameters).</summary>
    public static TypedParameterSpace Empty { get; } = new();
}
